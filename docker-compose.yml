version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./qr_codes:/app/qr_codes
      - ./data:/app/data
    restart: unless-stopped
    container_name: minor-project